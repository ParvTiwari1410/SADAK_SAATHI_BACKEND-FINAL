# =======================================================
# 🌐 Server settings (Render/Docker compatible)
# =======================================================
server.address=0.0.0.0
server.port=${PORT:8080}
spring.application.name=API

# =======================================================
# 🗄️ MySQL Database (Railway)
# =======================================================
spring.datasource.url=jdbc:mysql://caboose.proxy.rlwy.net:54521/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=tcPToZmElrHoDaJNIXzvmgqYRtasHUgR
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =======================================================
# 🧠 JPA / Hibernate
# =======================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.datasource.initialization-mode=always

